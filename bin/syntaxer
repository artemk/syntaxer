#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.expand_path(File.join(File.dirname(__FILE__), '..', 'lib')))

require 'rubygems'
require "open3"
require 'pp'
require 'syntaxer'

exit Syntaxer::Runner::CommandLine.run
